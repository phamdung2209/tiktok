const images = {
    logo: require('./logo.svg').default,
    messages: require('./messages.svg').default,
    inbox: require('./inbox.svg').default,
    noImage: require('./noImage.png'),
    noBg: require('./noBg.png')
}

export default images